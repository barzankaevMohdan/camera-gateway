# 🚀 Camera Gateway - Установка и запуск на Windows

Сервис для работы с видеопотоками IP-камер и стриминга.

---

## 📋 Требования

### 1. Node.js и npm
**Версия:** Node.js 18.x или выше

**Скачать:**
- [Node.js 18 LTS](https://nodejs.org/en/download/) (рекомендуется)
- [Node.js 20 LTS](https://nodejs.org/en/download/) (актуальная версия)

**Проверка установки:**
```powershell
node --version
npm --version
```

---

### 2. FFmpeg
**Для обработки видеопотоков с камер**

#### Вариант 1: Установка через Chocolatey (рекомендуется)

1. Установите Chocolatey (если еще не установлен):
```powershell
# Запустите PowerShell от имени администратора
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

2. Установите FFmpeg:
```powershell
choco install ffmpeg -y
```

#### Вариант 2: Ручная установка

1. Скачайте [FFmpeg для Windows](https://www.gyan.dev/ffmpeg/builds/)
2. Распакуйте архив (например, в `C:\ffmpeg`)
3. Добавьте в PATH:
   - Откройте **Панель управления** → **Система** → **Дополнительные параметры системы**
   - Нажмите **Переменные среды**
   - В разделе **Системные переменные** найдите `Path` и нажмите **Изменить**
   - Добавьте путь к папке `bin` (например, `C:\ffmpeg\bin`)

**Проверка установки:**
```powershell
ffmpeg -version
```

---

### 3. PostgreSQL (требуется для Prisma)
Camera Gateway использует ту же базу данных, что и Backend. Убедитесь, что PostgreSQL запущен в Docker.

---

## 🔧 Установка и настройка

### Шаг 1: Установка зависимостей

```powershell
cd camera-gateway
npm install
```

---

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` на основе `env.example`:
```powershell
copy env.example .env
```

Откройте `.env` и укажите:
```env
PORT=4000
NODE_ENV=development
DATABASE_URL=postgresql://attendance_user:secure_password_change_me@localhost:5432/attendance
FFMPEG_PATH=ffmpeg
ENCRYPTION_KEY=your-32-character-encryption-key
```

> ⚠️ **Важно:** `ENCRYPTION_KEY` должен быть **таким же**, как в backend!

---

### Шаг 3: Синхронизация Prisma схемы

```powershell
npm run prisma:copy-schema
npm run prisma:generate
```

---

## ▶️ Запуск

### Режим разработки
```powershell
npm run dev
```

### Продакшен
```powershell
npm run build
npm start
```

Сервис доступен на: `http://localhost:4000`

---

## 📹 Работа с камерами

### Доступные эндпоинты

- `GET /health` - Проверка состояния сервиса
- `GET /streams/:cameraId.mjpg` - MJPEG поток с камеры
- `GET /cameras/:cameraId/test` - Тест подключения к камере
- `GET /diagnostics` - Диагностика всех камер

### Пример RTSP URL

Для камер Hikvision:
```
rtsp://username:password@192.168.1.10:554/ISAPI/Streaming/Channels/101
```

Для камер Dahua:
```
rtsp://username:password@192.168.1.10:554/cam/realmonitor?channel=1&subtype=0
```

---

## 🛠️ Полезные команды

```powershell
# Обновление Prisma схемы из backend
npm run prisma:copy-schema
npm run prisma:generate

# Запуск с логами
npm run dev

# Сборка
npm run build

# Проверка типов
npm run type-check
```

---

## 🐛 Решение проблем

### Ошибка: "ffmpeg not found"
- Убедитесь, что FFmpeg установлен: `ffmpeg -version`
- Проверьте, что путь к FFmpeg добавлен в PATH
- Перезапустите PowerShell после установки FFmpeg
- Попробуйте указать полный путь в `.env`:
  ```env
  FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
  ```

### Ошибка: "Failed to open camera"
- Проверьте, что RTSP-поток камеры работает (используйте VLC)
- Убедитесь, что камера доступна в сети: `ping 192.168.1.10`
- Проверьте логин/пароль камеры
- Попробуйте открыть стрим напрямую:
  ```powershell
  ffmpeg -i "rtsp://admin:password@192.168.1.10:554/ISAPI/Streaming/Channels/101" -frames:v 1 test.jpg
  ```

### Ошибка: "Cannot connect to database"
- Убедитесь, что Backend запущен и база данных доступна
- Проверьте `DATABASE_URL` в `.env`

### Порт 4000 уже занят
```powershell
# Найти процесс на порту 4000
netstat -ano | findstr :4000

# Завершить процесс по PID
taskkill /PID <PID> /F
```

---

## 🏗️ Структура проекта

```
camera-gateway/
├── src/
│   ├── config.ts              # Конфигурация
│   ├── index.ts               # Точка входа
│   ├── prismaClient.ts        # Prisma клиент
│   ├── routes/                # API роуты
│   │   ├── cameras.ts
│   │   ├── diagnostics.ts
│   │   ├── health.ts
│   │   └── streams.ts
│   ├── services/              # Бизнес-логика
│   │   ├── cameraService.ts
│   │   └── streamManager.ts
│   └── utils/                 # Утилиты
│       └── encryption.ts
├── prisma/
│   └── schema.prisma          # Копия схемы из backend
├── scripts/
│   └── sync-prisma-schema.cjs # Скрипт синхронизации
├── .env                       # Переменные окружения
├── package.json
└── tsconfig.json
```

---

## 📚 Интеграция с другими сервисами

### Backend
Backend отправляет запросы в Camera Gateway для получения видеопотоков.

### Recognition Service
Recognition Service использует Camera Gateway для получения кадров с камер.

### Frontend
Admin и Client фронтенды отображают видео, получая URL стримов от Backend:
```
http://localhost:4000/streams/{camera_id}.mjpg
```

---

## 🎥 Поддерживаемые форматы камер

- **Hikvision** - протестировано ✅
- **Dahua** - протестировано ✅
- **Любые RTSP камеры** - должны работать
- **USB камеры** - не поддерживается (требуется доработка)

---

## 💡 Советы

1. **Используйте PowerShell 7** для лучшей совместимости
2. **Добавьте FFmpeg в исключения антивируса** для ускорения обработки видео
3. Для отладки используйте VLC Media Player для проверки RTSP потоков

---

Если возникли проблемы, откройте Issue в репозитории! 🚀
